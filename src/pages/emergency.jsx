import Layout from "../components/layout";
const Emergency = ({ collapse, setCollapse }) => {
  return (
    <Layout collapse={collapse} setCollapse={setCollapse}>
      <h1>Emergency</h1>
    </Layout>
  );
};
export default Emergency;
